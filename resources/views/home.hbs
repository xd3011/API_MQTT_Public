<div class="mt-4">
    <div class="row">
        {{#each house}}
            <div class="col-sm-6 col-lg-4">
                <div class="card" style="width: 18rem;">
                    <a href="#">
                        <img src="{{this.image}}" class="card-img-top" alt="{{this.name}}" />
                    </a>
                    <div class="card-body">
                        <a href="#">
                            <h5 class="card-title">{{this.house_name}}</h5>
                        </a>
                        <a href="#" class="btn btn-primary">Buy</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>